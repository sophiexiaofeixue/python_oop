#mutability and immutability

# mutation = change to object
# mutable, can by modified, like lists, sets, dictionaries
# immutable, can't by modified, like tuple and string

# example
a = [7, 3, 2, 1]
a[0] = 5

print(a)